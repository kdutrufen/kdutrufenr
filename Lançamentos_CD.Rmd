---
title: "Lançamentos_CD"
author: "Kdu Trufen"
date: "16 de fevereiro de 2019"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown
```{r}
library(httr)
library(jsonlite)

base_url <- "https://www.metal-archives.com/search/ajax-advanced/searching/albums/"


base_url <- "https://www.metal-archives.com/search/ajax-advanced/searching/albums/?bandName=&releaseTitle=&releaseYearFrom=2024&releaseMonthFrom=01&releaseYearTo=2024&releaseMonthTo=12&country=&location=&releaseLabelName=&releaseCatalogNumber=&releaseIdentifiers=&releaseRecordingInfo=&releaseDescription=&releaseNotes=&genre=-death+-grindcore+-groove+-black+-Sludge+-Djent+-Metalcore+-Deathcore+-Extreme+-Blackened+-Gorenoise&releaseType[]=1&releaseType[]=5#albums"

# make the GET request and store it in 'response'
response <- GET(url = base_url)

# Parse the JSON data:  
results <- fromJSON(content(response, as = "text"))

out <- readLines(con)
cat(out, sep = "\n")

# Parse the JSON data:  
results <- fromJSON(content(response, as = "text"))

# Extract the data table:
data_table <- results$aaData

albums_df <- as.data.frame(data_table, stringsAsFactors = FALSE)

colnames(albums_df) <- c("Band", "Album", "Type", "Genre", "Date") 
```
